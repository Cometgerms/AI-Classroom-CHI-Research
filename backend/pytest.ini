[pytest]
markers =
    unit: hardware-free unit tests
    simulation: simulated device integration
    hardware_optional: explicit physical-device integration; excluded unless selected
    optional_audio: audio conversion library tests (skip if dependencies absent)
addopts = -m "not hardware_optional"
